# OS
.DS_Store

# Env / secrets
.env
.env.*
!.env.example

# Node (frontend)
node_modules/
apps/web/.next/
apps/web/out/
apps/web/next-env.d.ts
*.tsbuildinfo

# Local operator play-harness output — holds generated wallet PRIVATE KEYS,
# game state, logs, results. NEVER commit.
scripts/out/
